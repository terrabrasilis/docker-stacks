version: '3.2'
services:
  webmap-homolog:
    image: terrabrasilis/webmap:hom_v2.2.1
    hostname: webmap.homologation
    networks:
      - proxy
    ports:
      - 80
    deploy:
      replicas: 1
      placement:
        constraints:
          - node.role==manager
      restart_policy:
        condition: on-failure

  deforestation-dashboard-homolog:
    image: terrabrasilis/deforestation-dashboard:v2.1.0
    hostname: deforestation.dashboard.homologation
    networks:
      - proxy
    ports:
      - 80
    deploy:
      replicas: 1
      placement:
        constraints:
          - node.role==manager
      restart_policy:
        condition: on-failure

networks:
  proxy:
    external: true
